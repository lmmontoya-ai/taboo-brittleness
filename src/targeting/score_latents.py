from __future__ import annotations

from typing import Any


def score_latents_by_corr(activations: Any, ll_secret: Any) -> list[int]:
    """Stub: rank latent features by correlation with LL(secret).
    Returns a list of latent indices sorted descending by score.
    """
    _ = (activations, ll_secret)
    return []

