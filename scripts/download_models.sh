#!/usr/bin/env bash
set -euo pipefail

# Download HF models and SAE weights.
# Note: This is a stub. Add `huggingface-cli` or `python -c` logic as needed.

echo "[download_models] Using models from models/taboo_list.txt (stub)"
echo "[download_models] Configure HF_HOME and auth if required."
